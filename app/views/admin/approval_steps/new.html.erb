<% content_for :title, "New step — Governauthzer" %>

<div class="max-w-5xl mx-auto">
  <%= render "admin/nav" %>
  <h1 class="page-title mb-6">
    Add step to <span class="text-gray-500"><%= @workflow.name %></span>
  </h1>
  <%= render "form", step: @step, url: admin_approval_workflow_approval_steps_path(@workflow) %>
</div>
