<% content_for :title, "Edit #{@workflow.name} — Governauthzer" %>

<div class="max-w-5xl mx-auto">
  <%= render "admin/nav" %>
  <h1 class="page-title mb-6">Edit <span class="text-gray-500"><%= @workflow.name %></span></h1>
  <%= render "form", workflow: @workflow %>
</div>
