<% content_for :title, "New application — Governauthzer" %>

<div class="w-full max-w-xl">
  <%= render "admin/nav" %>
  <h1 class="text-2xl font-semibold mb-6">Add application</h1>
  <%= render "form", application: @application %>
</div>
