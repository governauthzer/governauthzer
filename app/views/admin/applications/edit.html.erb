<% content_for :title, "Edit #{@application.name} — Governauthzer" %>

<div class="w-full max-w-5xl">
  <%= render "admin/nav" %>
  <h1 class="text-2xl font-semibold mb-6">Edit <span class="text-gray-500"><%= @application.name %></span></h1>
  <%= render "form", application: @application %>
</div>
