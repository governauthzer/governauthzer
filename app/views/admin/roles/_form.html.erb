<%= form_with model: role, url: url, scope: :role, local: true, class: "space-y-5 max-w-xl" do |f| %>
  <% if role.errors.any? %>
    <div class="p-3 rounded-lg bg-red-50 border border-red-200 text-red-800 text-sm space-y-1">
      <% role.errors.full_messages.each do |msg| %>
        <div>• <%= msg %></div>
      <% end %>
    </div>
  <% end %>

  <div>
    <%= f.label :name, class: "field-label" %>
    <%= f.text_field :name, required: true, class: input_class %>
  </div>

  <div>
    <%= f.label :slug, class: "field-label" %>
    <%= f.text_field :slug, required: true, class: "#{input_class} font-mono" %>
    <p class="field-hint">Unique within the application. Normalized to lowercase with dashes.</p>
  </div>

  <div>
    <%= f.label :approval_workflow_id, "Approval workflow", class: "field-label" %>
    <%= f.collection_select :approval_workflow_id, ApprovalWorkflow.order(:name), :id, :name,
                            { include_blank: "Default (manager approval)" }, class: input_class %>
    <p class="field-hint">Which workflow gates requests for this role.</p>
  </div>

  <div class="flex items-center gap-2">
    <%= f.check_box :protected %>
    <%= f.label :protected, "Protected — operator-assigned only, not self-requestable", class: "text-sm" %>
  </div>

  <div class="flex items-center gap-3 pt-2">
    <%= f.submit class: "btn btn-primary" %>
    <%= link_to "Cancel", admin_application_path(role.application || @application), class: "btn-quiet" %>
  </div>
<% end %>
