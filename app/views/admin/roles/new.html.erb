<% content_for :title, "New role — Governauthzer" %>

<div class="w-full max-w-5xl">
  <%= render "admin/nav" %>
  <h1 class="text-2xl font-semibold mb-6">
    New role in <span class="text-gray-500"><%= @application.name %></span>
  </h1>
  <%= render "form", role: @role, url: admin_application_roles_path(@application) %>
</div>
